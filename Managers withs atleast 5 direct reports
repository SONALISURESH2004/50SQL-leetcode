SELECT E1.name
FROM Employee E1
JOIN (
    SELECT managerId
    FROM Employee
    WHERE managerId IS NOT NULL
    GROUP BY managerId
    HAVING COUNT(*) >= 5
) AS Managers
ON E1.id = Managers.managerId;
